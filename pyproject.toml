[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "tsttools"
authors = [
  { name = "Brookhaven National Laboratory", email = "thopkins1@bnl.gov" },
]
description = "Tools for NSLS-II TST beamline"
readme = "README.md"
license.file = "LICENSE"
requires-python = ">=3.11,<3.14"
classifiers = [
  "Intended Audience :: Science/Research",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: BSD License",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering",
  "Typing :: Typed",
]
dynamic = ["version"]
dependencies = [
  "bluesky>=1.15.1",
  "ophyd-async[ca,pva]",
  "blop[cpu,ax,queueserver]",
]

[dependency-groups]
dev = [
  "ophyd-async[ca,sim]",
  "import-linter",
  "numpydoc",
  "pipdeptree",
  "pre-commit",
  "pyright",
  "pytest",
  "pytest-asyncio",
  "pytest-cov",
  "pytest-mock",
  "ruff",
  "tiled[client,server]>=0.2.14",
  "bluesky-tiled-plugins>=2.0.9",
]
docs = []

[project.urls]
Homepage = "https://github.com/thopkins32/tsttools"
Issues = "https://github.com/thopkins32/tsttools/issues"
Discussions = "https://github.com/thopkins32/tsttools/discussions"
Changelog = "https://github.com/thopkins32/tsttools/releases"

[tool.hatch]
version.source = "vcs"

[tool.hatch.build]
artifacts = ["src/tsttools/_version.py"]

[tool.hatch.build.targets.wheel]
sources = ["src"]

[tool.hatch.build.hooks.vcs]
version-file = "src/tsttools/_version.py"

[tool.hatch.version.raw-options]
local_scheme = "no-local-version"

[tool.pyright]
typeCheckingMode = "standard"

[tool.coverage.paths]
# Tests are run from installed location, map back to the src directory
source = ["src"]

[tool.ruff]
line-length = 125
lint.select = [
  "B",       # flake8-bugbear - https://docs.astral.sh/ruff/rules/#flake8-bugbear-b
  "C4",      # flake8-comprehensions - https://docs.astral.sh/ruff/rules/#flake8-comprehensions-c4
  "E",       # pycodestyle errors - https://docs.astral.sh/ruff/rules/#error-e
  "F",       # pyflakes rules - https://docs.astral.sh/ruff/rules/#pyflakes-f
  "N",       # pep8-naming - https://docs.astral.sh/ruff/rules/#pep8-naming-n
  "W",       # pycodestyle warnings - https://docs.astral.sh/ruff/rules/#warning-w
  "I",       # isort - https://docs.astral.sh/ruff/rules/#isort-i
  "UP",      # pyupgrade - https://docs.astral.sh/ruff/rules/#pyupgrade-up
  "SLF",     # self - https://docs.astral.sh/ruff/settings/#lintflake8-self
  "PLC2701", # private import - https://docs.astral.sh/ruff/rules/import-private-name/
  "LOG015",  # root logger call - https://docs.astral.sh/ruff/rules/root-logger-call/
  "S101",    # assert - https://docs.astral.sh/ruff/rules/assert/
  "D",       # docstring - https://docs.astral.sh/ruff/rules/#pydocstyle-d
]
lint.ignore = [
  "B901", # Return in a generator is needed for plans
  "D107", # We squash __init__ docstrings into the class docstring
  "D102", # We don't need to override docstrings that are inherited from abstract methods
]
lint.preview = true # so that preview mode PLC2701 is enabled

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.per-file-ignores]
# By default, private member access is allowed in tests
# See https://github.com/DiamondLightSource/python-copier-template/issues/154
# Remove this line to forbid private member access in tests
"tests/**/*" = ["SLF001", "S101", "D"]

[tool.importlinter]
root_package = "tsttools"
